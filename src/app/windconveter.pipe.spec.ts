import { WindconveterPipe } from './windconveter.pipe';

describe('WindconveterPipe', () => {
  it('create an instance', () => {
    const pipe = new WindconveterPipe();
    expect(pipe).toBeTruthy();
  });
});
